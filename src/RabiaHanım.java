import java.util.Map;

public class RabiaHanım {

    // 4. Metod: computeIfAbsent() ile değer ekleme
    public static void addWithComputeIfAbsent(Map<String, Integer> map, String key, Integer value) {
        // Yapılacak iş
    }
}
